import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  safeContainer: {
    flex: 1,
  },
  splashBG: {
    flex: 1,
    resizeMode: "contain",
  },
});

export default styles;
